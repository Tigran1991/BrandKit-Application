import React from "react";
import * as Styled from '../styled';

const EmptyArea = () => {

    return (
        <Styled.EmptyArea>
            <p> You do not have collections of the selected brandkit yet </p>
        </Styled.EmptyArea>
    )
}

export default EmptyArea;